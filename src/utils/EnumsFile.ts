export enum StatusEnum {
  ERROR = "error",
  SUCCESS = "success",
  IDLE = "idle",
  LOADING = "loading",
  SUCCEEDED = "succeeded",
  FAILED = "failed",
  FULFILLED = "fulfilled",
}

export enum RoutePaths {
  MAIN = "/",
  ADMIN = "/admin"
}
